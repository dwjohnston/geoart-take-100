import { Typography } from '@material-ui/core';
import React from 'react';

export type HeaderProps = {
    
}

export const Header = (props: HeaderProps) => {
  const {} = props;
  return <>
        <Typography variant="h1">GeoPlanets - interactive geometric art</Typography>
  </>;
};
